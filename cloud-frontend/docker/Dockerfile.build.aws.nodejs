ARG BASE_IMAGE
FROM ${BASE_IMAGE}
ARG USER
ENV HOME=/home/${USER}

RUN useradd --uid 1000 ${USER}
WORKDIR ${HOME}
USER ${USER}:${USER}

CMD cd /mnt/function && npm install && zip -qur ${APP}.zip node_modules && rm -rf node_modules package-lock.json
