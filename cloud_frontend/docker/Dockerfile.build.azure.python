ARG BASE_IMAGE
FROM ${BASE_IMAGE}
ARG USER
ARG VERSION
ENV HOME=/home/${USER}
ENV PYTHON_VERSION=${VERSION}

RUN useradd --uid 1000 ${USER}
WORKDIR ${HOME}
USER ${USER}:${USER}

ENV SCRIPT_FILE=/mnt/function/package.sh
CMD cd /mnt/function\
    && pip3 -q install -r requirements.txt -r requirements.txt.${PYTHON_VERSION} --target=.python_packages/lib/site-packages\
    && if test -f "${SCRIPT_FILE}"; then /bin/bash ${SCRIPT_FILE} .python_packages/lib/site-packages ; fi
