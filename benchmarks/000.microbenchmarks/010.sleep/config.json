{
  "timeout": 120,
  "memory": 128,
  "languages": ["python", "nodejs"],
  "trigger": {
    "type": "TIMER",
    "params": {
      "pattern": "rate(1 hour)",
      "ruleName": "testRule"
    }
  }
}
