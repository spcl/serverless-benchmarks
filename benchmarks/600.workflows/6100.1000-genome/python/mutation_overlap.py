import os
import io
import uuid
import re
from . import storage


def handler(event):
  #individuals_merge output is event[0], sifting_output is event[1]? 
  #event["sifting"] supposed to have sifting output.
  #event["individuals_merge"] supposed to have merge output. 
  

  return event
